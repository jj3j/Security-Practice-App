name: CI

on:
  push:
  pull_request:
  workflow_call:

permissions:
  contents: read

jobs:
  validate:
    name: Validate application
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install backend dependencies
        run: python -m pip install --requirement backend/requirements.txt

      - name: Validate repository data and configuration
        run: python scripts/validate_repo.py

      - name: Validate immutable release archive contract
        run: |
          git archive --format=tar.gz --output=release.tar.gz HEAD
          bash deploy/scripts/validate-release-archive.sh release.tar.gz

      - name: Compile Python sources
        run: python -m compileall -q backend scripts
